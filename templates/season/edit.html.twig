{% extends 'base.html.twig' %}

{% block title %}Edit Season{% endblock %}

{% block body %}
    <h1> {{ 'update.season'|trans}}</h1>

    {{ include('season/_form.html.twig', {'button_label': ' {{ 'update'|trans}}'}) }}

    <a href="{{ path('season_index') }}"> {{ 'back.list'|trans}}</a>

    {{ include('season/_delete_form.html.twig') }}
{% endblock %}
